# 作业代码整理与修正

## 作业1 - 基础输入输出

### 1.1 十进制转八进制和十六进制
**题目要求：** 读入1个10进制正整数，分别以8进制和16进制形式输出，不加前缀，16进制字母小写。

```c
#include <stdio.h>

int main() 
{
    int a;
    scanf("%d", &a);
    printf("%o %x", a, a);  // %o输出八进制，%x输出十六进制（小写）
    return 0;
}
```

### 1.2 输出变量值和字节数
**题目要求：** 输入字符、整数和实数，分别输出它们的值和所占内存字节数。

```c
#include <stdio.h>

int main() 
{
    char a;
    int b;
    double c;
    scanf("%c %d %lf", &a, &b, &c);
    printf("%c,%zu\n", a, sizeof(a));
    printf("%d,%zu\n", b, sizeof(b));
    printf("%.6f,%zu", c, sizeof(c));
    return 0;
}
```

## 作业2 - 基础运算和转换

### 2.1 计算两个整数的乘积
**题目要求：** 计算两个整数A和B的乘积（-50000 ≤ A, B ≤ 50000）。

```c
#include <stdio.h>

int main() 
{
    long long A, B;
    scanf("%lld %lld", &A, &B);
    printf("%lld", A * B);
    return 0;
}
```

### 2.2 三角形面积计算
**题目要求：** 输入三角形三边长度，使用海伦公式计算面积和周长。

```c
#include <stdio.h>
#include <math.h>

int main() 
{
    double a, b, c;
    scanf("%lf %lf %lf", &a, &b, &c);
    
    double circumference = a + b + c;                    // 周长
    double s = circumference / 2;                       // 半周长
    double area = sqrt(s * (s - a) * (s - b) * (s - c)); // 海伦公式
    
    printf("circumference = %.2f, area = %.2f", circumference, area);
    return 0;
}
```

### 2.3 字符ASCII码转换
**题目要求：** 输入一个字符，输出其对应的ASCII码。

```c
#include <stdio.h>

int main() 
{
    char a;
    scanf("%c", &a);
    printf("%d", a);
    return 0;
}
```

### 2.4 简单字母加密
**题目要求：** 输入ad或AD范围内的字母，输出其后继字母。

```c
#include <stdio.h>

int main() 
{
    char a;
    scanf("%c", &a);
    char b = a + 1;
    printf("%c", b);
    return 0;
}
```

### 2.5 ASCII码转字符
**题目要求：** 将给定的ASCII码转换为对应的字符输出。

```c
#include <stdio.h>

int main() 
{
    // 104=h, 101=e, 108=l, 108=l, 111=o
    printf("%c%c%c%c%c", 104, 101, 108, 108, 111);
    return 0;
}
```

## 作业3 - 条件判断和算法

### 3.1 数字解密
**题目要求：** 对加密数字进行解密，加密规则是原始数字加8后取个位。

```c
#include <stdio.h>

int main() 
{
    int a;
    scanf("%d", &a);
    
    // 解密逻辑：如果a>=8，则原始数字=a-8；否则原始数字=a+2
    if (a >= 8) 
    {
        printf("%d", a - 8);
    }
    else 
    {
        printf("%d", a + 2);
    }
    return 0;
}
```

### 3.2 苹果和虫子问题
**题目要求：** 计算经过y小时后剩下的完整苹果数量。

```c
#include <stdio.h>

int main() 
{
    long long n, x, y;
    scanf("%lld %lld %lld", &n, &x, &y);
    
    // 计算被吃掉的完整苹果数
    long long eaten_apples = y / x;
    if (y % x != 0)  // 如果时间不够吃完一个，也算吃了一个
    {
        eaten_apples++;
    }
    
    long long remaining = n - eaten_apples;
    if (remaining < 0) 
    {
        remaining = 0;
    }
    printf("%lld", remaining);
    return 0;
}
```

### 3.3 一门课不及格判断
**题目要求：** 判断学生是否恰好有一门课不及格。

```c
#include <stdio.h>

int main() 
{
    int chinese, math;
    scanf("%d %d", &chinese, &math);
    
    // 恰好有一门不及格：一门<60且另一门>=60
    if ((chinese < 60 && math >= 60) || (chinese >= 60 && math < 60)) 
    {
        printf("1");
    }
    else 
    {
        printf("0");
    }
    return 0;
}
```

### 3.4 四个整数排序
**题目要求：** 输入四个整数，按从小到大顺序输出。

```c
#include <stdio.h>

int main() 
{
    int a[4];
    
    // 输入四个整数
    for (int i = 0; i < 4; i++) 
    {
        scanf("%d", &a[i]);
    }
    
    // 冒泡排序
    for (int i = 0; i < 3; i++) 
    {
        for (int j = 0; j < 3 - i; j++) 
        {
            if (a[j] > a[j + 1]) 
            {
                int temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }
    
    // 输出结果
    for (int i = 0; i < 3; i++) 
    {
        printf("%d ", a[i]);
    }
    printf("%d", a[3]);
    return 0;
}
```

### 3.5 出租车计价
**题目要求：** 根据行驶里程和等待时间计算车费。

```c
#include <stdio.h>
#include <math.h>

int main() 
{
    double distance;  // 行驶里程
    int wait_time;    // 等待时间
    scanf("%lf %d", &distance, &wait_time);
    
    double fee = 0;
    
    // 里程费计算
    if (distance <= 3) 
    {
        fee = 10;  // 起步费
    }
    else if (distance <= 10) 
    {
        fee = 10 + (distance - 3) * 2;  // 3-10公里内每公里2元
    }
    else 
    {
        fee = 10 + 7 * 2 + (distance - 10) * 3;  // 10公里以上每公里3元
    }
    
    // 等待费：每5分钟2元
    fee += (wait_time / 5) * 2;
    
    // 四舍五入输出
    printf("%.0f", round(fee));
    return 0;
}
```

### 3.6 数组统计和排序
**题目要求：** 输入n个整数，计算总和和平均值，排序后输出最大值、最小值和平均值。

```c
#include <stdio.h>

int main() 
{
    int n;
    double sum = 0.0;
    scanf("%d", &n);
    
    int a[n];
    
    // 输入数组并计算总和
    for (int i = 0; i < n; i++) 
    {
        scanf("%d", &a[i]);
        sum += a[i];
    }
    
    // 冒泡排序（升序）
    for (int i = 0; i < n - 1; i++) 
    {
        for (int j = 0; j < n - 1 - i; j++) 
        {
            if (a[j] > a[j + 1]) 
            {
                int temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }
    
    // 输出最大值、最小值和平均值
    printf("%d\n%d\n%.2f", a[n - 1], a[0], sum / n);
    return 0;
}
```

### 3.7 整数反转
**题目要求：** 输入一个正整数，输出反转后的数。

```c
#include <stdio.h>

int main() 
{
    int n;
    scanf("%d", &n);
    
    int reversed = 0;
    while (n > 0) 
    {
        reversed = reversed * 10 + n % 10;  // 将n的个位数加到反转数末尾
        n /= 10;                            // 去掉n的个位数
    }
    
    printf("%d", reversed);
    return 0;
}
```



所有代码都经过测试，能够正确解决对应的问题。
